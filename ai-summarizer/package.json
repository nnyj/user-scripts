{
  "name": "ai-summarizer",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "build:userscript": "node build.js --userscript",
    "build:extension": "node build.js --extension"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "dompurify": "^3.2.4",
    "marked": "^15.0.7",
    "mermaid": "^11.4.1"
  }
}
